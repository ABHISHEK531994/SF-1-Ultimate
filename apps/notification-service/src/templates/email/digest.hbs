<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 30px; text-align: center; }
    .content { background: #f9f9f9; padding: 30px; }
    .notification-item { background: white; padding: 15px; margin: 10px 0; border-radius: 5px; border-left: 3px solid #667eea; }
    .notification-title { font-weight: bold; margin-bottom: 5px; }
    .notification-time { font-size: 12px; color: #999; }
    .button { display: inline-block; padding: 12px 30px; background: #667eea; color: white; text-decoration: none; border-radius: 5px; margin-top: 20px; }
    .footer { text-align: center; padding: 20px; color: #999; font-size: 12px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>📬 {{count}} neue Benachrichtigungen</h1>
    </div>
    
    <div class="content">
      <p>Hier ist deine Zusammenfassung:</p>
      
      {{#each notifications}}
        <div class="notification-item">
          <div class="notification-title">{{this.title}}</div>
          <div>{{this.message}}</div>
          <div class="notification-time">{{formatDate this.createdAt}}</div>
          {{#if this.url}}
            <a href="{{this.url}}" style="color: #667eea; text-decoration: none;">Ansehen →</a>
          {{/if}}
        </div>
      {{/each}}
      
      <a href="https://seedfinderpro.de/notifications" class="button">Alle Benachrichtigungen</a>
    </div>
    
    <div class="footer">
      <p>Du erhältst diesen Digest, weil du ihn in deinen Einstellungen aktiviert hast.</p>
      <p><a href="https://seedfinderpro.de/settings/notifications">Einstellungen ändern</a></p>
    </div>
  </div>
</body>
</html>