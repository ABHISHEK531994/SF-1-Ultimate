<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%); color: white; padding: 30px; text-align: center; }
    .content { background: #f9f9f9; padding: 30px; }
    .price-box { background: white; padding: 20px; border-radius: 8px; margin: 20px 0; border-left: 4px solid #11998e; }
    .old-price { text-decoration: line-through; color: #999; }
    .new-price { font-size: 24px; font-weight: bold; color: #11998e; }
    .discount { background: #38ef7d; color: white; padding: 5px 10px; border-radius: 3px; display: inline-block; margin-left: 10px; }
    .button { display: inline-block; padding: 12px 30px; background: #11998e; color: white; text-decoration: none; border-radius: 5px; margin-top: 20px; }
    .footer { text-align: center; padding: 20px; color: #999; font-size: 12px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>💰 Preisalarm!</h1>
    </div>
    
    <div class="content">
      <h2>{{strainName}}</h2>
      <p>{{message}}</p>
      
      <div class="price-box">
        {{#if oldPrice}}
          <div class="old-price">Vorher: {{oldPrice}}€</div>
        {{/if}}
        <div class="new-price">
          {{newPrice}}€
          {{#if discount}}
            <span class="discount">-{{discount}}%</span>
          {{/if}}
        </div>
        <div style="margin-top: 10px; color: #666;">
          bei {{seedbank}}
        </div>
      </div>
      
      {{#if url}}
        <a href="{{url}}" class="button">Jetzt kaufen</a>
      {{/if}}
    </div>
    
    <div class="footer">
      <p>Du erhältst diese E-Mail, weil du einen Preisalarm für diesen Strain aktiviert hast.</p>
      <p><a href="https://seedfinderpro.de/settings/alerts">Preisalarme verwalten</a></p>
    </div>
  </div>
</body>
</html>